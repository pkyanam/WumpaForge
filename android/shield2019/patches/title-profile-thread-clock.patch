--- a/graphics_timing_probe.inc
+++ b/graphics_timing_probe.inc
@@ -23,9 +23,13 @@ struct WrathFrameProfile {
     uint64_t surface_count[3],surface_bytes[3],surface_time[3];
     unsigned frames,intervals;
 };
-static struct WrathFrameProfile wrath_profiles[2];
+/* CPU timestamps belong to one host thread, not the "worker" role. Loading
+ * workers may be replaced or alternate; sharing last_cpu across them produces
+ * unsigned underflow and mixes unrelated frame intervals. TLS also starts a
+ * fresh interval history when a worker exits and its host identity is reused. */
+static _Thread_local struct WrathFrameProfile wrath_profile_thread;
 static struct WrathFrameProfile *wrath_profile_current(void)
-{ return &wrath_profiles[window_thread()?0:1]; }
+{ return &wrath_profile_thread; }
 static uint64_t wrath_profile_begin(void)
 { return wrath_profile_enabled()?wrath_profile_clock(CLOCK_MONOTONIC):0; }
 static void wrath_profile_upload(uint64_t start,uint64_t rgba_bytes,
