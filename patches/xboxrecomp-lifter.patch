diff --git a/templates/runtime/recomp_types.h b/templates/runtime/recomp_types.h
index 4cf3d79..ad5375f 100644
--- a/templates/runtime/recomp_types.h
+++ b/templates/runtime/recomp_types.h
@@ -60,6 +60,8 @@
  * CMakeLists) -- MSVC's C4013 was emitted and discarded. Same failure as the
  * missing stdlib.h in kernel_bridge.c, in a hotter path. */
 #include <math.h>
+/* Development builds stop when reachable code is not yet lifted. */
+void recomp_unsupported_instruction(uint32_t xbox_va);

 /* MSVC's __forceinline -> gcc/clang equivalent on POSIX. */
 #if !defined(_MSC_VER) && !defined(__forceinline)
@@ -244,6 +246,11 @@ extern RECOMP_TLS int g_df;
    word has to survive a call. (g_fp_stack/g_fp_top are declared above.) */
 extern RECOMP_TLS uint16_t g_fp_control_word;
 extern RECOMP_TLS int g_fp_cmp;
+/* Separate x87 status from the comparison snapshot used by EFLAGS branches.
+ * TOP comes from g_fp_top. Empty bits track physical registers, with 1=empty.
+ * This does not add 80-bit arithmetic or arithmetic exception generation. */
+extern RECOMP_TLS uint16_t g_fp_status_word;
+extern RECOMP_TLS uint8_t g_fp_empty_mask;

 /* Result of an x87 compare, in the shape the status word wants:
  *   -1 less, 0 equal, 1 greater, 2 unordered (either operand is NaN).
@@ -251,6 +258,58 @@ extern RECOMP_TLS int g_fp_cmp;
  * followed by `test ah, 0x44; jp` is how this era's CRT asks "is this a NaN",
  * and collapsing it to "equal" answers no every time. */
 #define RECOMP_FCMP(a, b)     (((a) != (a) || (b) != (b)) ? 2 : (a) < (b) ? -1 : (a) > (b) ? 1 : 0)
+#define RECOMP_FP_COMPARE_STATUS() do { \
+    g_fp_status_word = (uint16_t)((g_fp_status_word & ~0x4700u) | \
+        (g_fp_cmp == 2 ? 0x4500u : g_fp_cmp < 0 ? 0x0100u : \
+         g_fp_cmp > 0 ? 0u : 0x4000u)); \
+} while (0)
+#define RECOMP_FP_STATUS() ((uint16_t)((g_fp_status_word & ~0x3800u) | \
+                                     ((g_fp_top & 7u) << 11)))
+/* FPREM/FPREM1 in the existing binary64 execution model, not x87 extended
+ * precision. Each large-exponent step uses Intel's permitted N=32 reduction.
+ * Invalid/NaN inputs need unmodeled x87 exception semantics: fail explicitly. */
+static inline double recomp_fp_remainder(double x, double y, int nearest, uint32_t address)
+{
+    if (isnan(x) || isnan(y) || isinf(x) || y == 0.0) {
+        recomp_unsupported_instruction(address);
+        return NAN; /* diagnostic is non-returning in the runtime */
+    }
+    if (x == 0.0 || isinf(y)) {
+        g_fp_status_word &= (uint16_t)~0x4700u;
+        return x;
+    }
+    int difference = ilogb(fabs(x)) - ilogb(fabs(y));
+    if (difference >= 64) {
+        double scaled = scalbn(y, difference - 32);
+        /* C0/C1/C3 are undefined on an incomplete step; retain their bits. */
+        g_fp_status_word |= 0x0400u;
+        double result = fmod(x, scaled);
+        return result == 0.0 ? copysign(0.0, x) : result;
+    }
+    int quotient;
+    double near = remquo(fabs(x), fabs(y), &quotient);
+    /* The condition bits encode the quotient magnitude, not its sign.
+     * remquo chooses nearest-even; a negative remainder rounded up and must
+     * decrement the magnitude for FPREM's truncation toward zero. */
+    unsigned low = (unsigned)quotient;
+    if (!nearest && near < 0.0) --low;
+    uint16_t condition = (uint16_t)(((low & 4u) << 6) |
+                                   ((low & 2u) << 13) | ((low & 1u) << 9));
+    g_fp_status_word = (g_fp_status_word & (uint16_t)~0x4700u) | condition;
+    double result = nearest ? remainder(x, y) : fmod(x, y);
+    return result == 0.0 ? copysign(0.0, x) : result;
+}
+
+static inline void recomp_fp_examine(void)
+{
+    double value = g_fp_stack[g_fp_top & 7u];
+    unsigned category = (g_fp_empty_mask & (1u << (g_fp_top & 7u))) ? 0x4100u :
+        isnan(value) ? 0x0100u : isinf(value) ? 0x0500u : value == 0 ? 0x4000u : 0x0400u;
+    /* Every nonzero finite binary64 value is normal in x87's extended range,
+     * including a binary64 subnormal loaded by FLD. C1 reports sign even empty. */
+    g_fp_status_word = (uint16_t)((g_fp_status_word & ~0x4700u) | category |
+                                 (signbit(value) ? 0x0200u : 0u));
+}

 /* ================================================================
  * ICALL trace ring buffer (for debugging indirect calls)
@@ -574,6 +633,95 @@ static inline uint32_t SUB32_CF(uint32_t a, uint32_t b, int *cf) {
  * Rotation / shift helpers
  * ================================================================ */

+/* Operand-width rotation; counts use the x86 five-bit mask. OF is defined
+ * only when that masked count is 1. Zero count leaves both output flags alone. */
+static inline uint32_t RECOMP_ROTATE(uint32_t value, unsigned count, unsigned bits,
+                                    int right, int *cf, int *of)
+{
+    uint32_t mask = bits == 32 ? UINT32_MAX : (1u << bits) - 1u;
+    value &= mask;
+    unsigned masked = count & 31u, shift = masked & (bits - 1u);
+    uint32_t result = value;
+    if (shift) result = right ? (value >> shift) | (value << (bits - shift)) :
+                               (value << shift) | (value >> (bits - shift));
+    result &= mask;
+    if (masked) {
+        unsigned carry = right ? result >> (bits - 1u) : result & 1u;
+        if (cf) *cf = (int)carry;
+        if (of && masked == 1u)
+            *of = (int)(((result >> (bits - 1u)) ^
+                         (right ? result >> (bits - 2u) : carry)) & 1u);
+    }
+    return result;
+}
+/* Rotate the operand and CF as a 9/17/33-bit ring. The count is masked to 5
+ * bits before reducing modulo the ring width. OF is defined only at count 1. */
+static inline uint32_t RECOMP_CARRY_ROTATE(uint32_t value, unsigned count, unsigned bits,
+                                          int right, int *cf, int *of)
+{
+    uint32_t mask = bits == 32 ? UINT32_MAX : (1u << bits) - 1u;
+    unsigned masked = count & 31u, shift = masked % (bits + 1u);
+    uint64_t ring_mask = (UINT64_C(1) << (bits + 1u)) - 1u;
+    uint64_t ring = (value & mask) | ((uint64_t)(*cf & 1) << bits);
+    if (shift) ring = (right ? (ring >> shift) | (ring << (bits + 1u - shift)) :
+                              (ring << shift) | (ring >> (bits + 1u - shift))) & ring_mask;
+    uint32_t result = (uint32_t)ring & mask;
+    *cf = (int)((ring >> bits) & 1u);
+    if (of && masked == 1u)
+        *of = (int)(((result >> (bits - 1u)) ^
+                     (right ? result >> (bits - 2u) : (unsigned)*cf)) & 1u);
+    return result;
+}
+
+/* Logical shifts truncate to operand width before shifting. CF is undefined
+ * for nonzero masked counts >= width; choose 0 there. OF is defined at count 1. */
+static inline uint32_t RECOMP_SHIFT(uint32_t value, unsigned count, unsigned bits,
+                                   int right, int *cf, int *of)
+{
+    uint32_t mask = bits == 32 ? UINT32_MAX : (1u << bits) - 1u;
+    value &= mask; count &= 31u;
+    if (!count) return value;
+    unsigned carry = count < bits ?
+        (right ? (value >> (count - 1u)) & 1u : (value >> (bits - count)) & 1u) : 0;
+    uint32_t result = count >= bits ? 0 :
+        (right ? value >> count : (value << count) & mask);
+    if (cf) *cf = (int)carry;
+    if (of && count == 1u)
+        *of = right ? (int)(value >> (bits - 1u)) :
+                      (int)((result >> (bits - 1u)) ^ carry);
+    return result;
+}
+/* SHLD/SHRD: counts above 16 for 16-bit operands have undefined result/flags;
+ * choose the unchanged value there. Count 0 always preserves all state. */
+static inline uint32_t RECOMP_DOUBLE_SHIFT(uint32_t value, uint32_t source,
+                                          unsigned count, unsigned bits,
+                                          int right, int *cf, int *of)
+{
+    uint32_t mask = bits == 32 ? UINT32_MAX : (1u << bits) - 1u;
+    value &= mask; source &= mask; count &= 31u;
+    if (!count || count > bits) return value;
+    unsigned carry = right ? (value >> (count - 1u)) & 1u :
+                             (value >> (bits - count)) & 1u;
+    uint32_t result = count == bits ? source : right ?
+        (value >> count) | ((source << (bits - count)) & mask) :
+        ((value << count) & mask) | (source >> (bits - count));
+    if (cf) *cf = (int)carry;
+    if (of && count == 1u)
+        *of = right ? (int)((value ^ result) >> (bits - 1u)) :
+                      (int)((result >> (bits - 1u)) ^ carry);
+    return result;
+}
+static inline uint32_t RECOMP_SAR(uint32_t value, unsigned count, unsigned bits, int *cf)
+{
+    uint32_t mask = bits == 32 ? UINT32_MAX : (1u << bits) - 1u;
+    value &= mask;count &= 31u;
+    if (!count) return value;
+    uint32_t sign = value >> (bits - 1u);
+    if (cf) *cf = count >= bits ? (int)sign : (int)((value >> (count - 1u)) & 1u);
+    if (count >= bits) return sign ? mask : 0;
+    return (value >> count) | (sign ? mask ^ (mask >> count) : 0);
+}
+
 static inline uint32_t ROL32(uint32_t val, int n) {
     n &= 31;
     return (val << n) | (val >> (32 - n));
@@ -886,9 +1034,8 @@ void recomp_abi_violation_log(uint32_t va, uint32_t ebx0, uint32_t esi0,
  *
  * mm0..mm7 alias the x87 stack on real hardware. Nothing here does, and
  * nothing needs to: a title that interleaves the two calls emms between, and
- * emms is a no-op for us. Modelling the aliasing would mean giving up the
- * separate x87 model that the FPU work depends on, to reproduce a hazard the
- * hardware exists to let software avoid.
+ * EMMS empties the x87 tags; the register values remain separate. MMX/x87
+ * value aliasing and MMX writes marking x87 registers valid remain unmodeled.
  * ================================================================ */

 #ifndef RECOMP_MMX_DEFINED
diff --git a/tools/conformance/__main__.py b/tools/conformance/__main__.py
index aa3c9f9..f87c87a 100644
--- a/tools/conformance/__main__.py
+++ b/tools/conformance/__main__.py
@@ -124,7 +124,10 @@ def _lift(code_bytes):
     # FunctionTranslator sets this per function, and the lifter only bothers
     # producing CF when something consumes it. Without it the snippet would be
     # lifted differently from how recomp would lift the same bytes.
-    lifter.needs_cf = any(i.mnemonic in ("sbb", "adc") for i in insns)
+    from tools.recomp.translator import FunctionTranslator
+    lifter.needs_cf = FunctionTranslator._function_needs_cf(insns)
+    lifter.needs_shift_snapshot = any(i.mnemonic in ("shl", "shr", "shld", "shrd") for i in insns)
+    lifter.needs_rotate_snapshot = any(i.mnemonic in ("rol", "ror", "rcl", "rcr") for i in insns)
     lines, _ = lift_basic_block(lifter, BasicBlock(start=0x00100000,
                                                    instructions=insns))
     return list(lines), mnemonics
diff --git a/tools/conformance/corpus_run.py b/tools/conformance/corpus_run.py
index 69145dd..ef03b6d 100644
--- a/tools/conformance/corpus_run.py
+++ b/tools/conformance/corpus_run.py
@@ -163,6 +163,7 @@ RECOMP_TLS uint32_t g_seh_ebp, g_ebp;
 RECOMP_TLS int g_df;   /* EFLAGS.DF: the direction the string ops walk */
 RECOMP_TLS double g_fp_stack[8]; RECOMP_TLS int g_fp_top;
 RECOMP_TLS uint16_t g_fp_control_word = 0x027F; RECOMP_TLS int g_fp_cmp;
+RECOMP_TLS uint16_t g_fp_status_word; RECOMP_TLS uint8_t g_fp_empty_mask = 0xFF;
 RECOMP_TLS RecompXmm g_xmm0,g_xmm1,g_xmm2,g_xmm3,g_xmm4,g_xmm5,g_xmm6,g_xmm7;
 RECOMP_TLS RecompMmx g_mm0,g_mm1,g_mm2,g_mm3,g_mm4,g_mm5,g_mm6,g_mm7;
 volatile uint32_t g_icall_trace[16]; volatile uint32_t g_icall_trace_idx;
@@ -199,7 +200,7 @@ static int g_total, g_fail;
    recompiler's own generated code assumes. Setting that up here is the point:
    it exercises the real calling path rather than a special one. */
 static void enter(void) {
-    g_fp_top = 0; g_fp_cmp = 0; g_fp_control_word = 0x027Fu;
+    g_fp_top = 0; g_fp_cmp = 0; g_fp_status_word = 0; g_fp_empty_mask = 0xFF; g_fp_control_word = 0x027Fu;
     memset(g_fp_stack, 0, sizeof g_fp_stack);
     g_eax = g_ecx = g_edx = g_ebx = g_esi = g_edi = 0;
     g_seh_ebp = g_ebp = 0;
diff --git a/tools/conformance/harness.py b/tools/conformance/harness.py
index 0d8eece..7ad5c0a 100644
--- a/tools/conformance/harness.py
+++ b/tools/conformance/harness.py
@@ -104,6 +104,7 @@ RECOMP_TLS uint32_t g_seh_ebp, g_ebp;
 RECOMP_TLS int g_df;   /* EFLAGS.DF: the direction the string ops walk */
 RECOMP_TLS double g_fp_stack[8]; RECOMP_TLS int g_fp_top;
 RECOMP_TLS uint16_t g_fp_control_word = 0x027F; RECOMP_TLS int g_fp_cmp;
+RECOMP_TLS uint16_t g_fp_status_word; RECOMP_TLS uint8_t g_fp_empty_mask = 0xFF;
 RECOMP_TLS RecompXmm g_xmm0,g_xmm1,g_xmm2,g_xmm3,g_xmm4,g_xmm5,g_xmm6,g_xmm7;
 RECOMP_TLS RecompMmx g_mm0,g_mm1,g_mm2,g_mm3,g_mm4,g_mm5,g_mm6,g_mm7;
 volatile uint32_t g_icall_trace[16]; volatile uint32_t g_icall_trace_idx;
@@ -133,7 +134,7 @@ static unsigned char l_xmm[128];

 _LIFTED_PROLOGUE = {
     "gpr": """    g_eax = g_in_a; g_ecx = g_in_b; g_edx = 0;""",
-    "fpu": """    g_fp_top = 0; g_fp_control_word = 0x027Fu; g_fp_cmp = 0;
+    "fpu": """    g_fp_top = 0; g_fp_control_word = 0x027Fu; g_fp_cmp = 0; g_fp_status_word = 0; g_fp_empty_mask = 0xFF;
 @FPMACROS@
     memset(g_fp_stack, 0, sizeof(g_fp_stack));
     g_eax = (uint32_t)(uintptr_t)g_scratch; g_ecx = 0; g_edx = 0;""",
@@ -270,6 +271,10 @@ def harness_source(prepared, why_of, tol_of):
         body = "\n".join(f"    {l}" for l in lines) or "    /* nothing */"
         out.append(f"""static void lif_{name}(void) {{
     uint32_t ebp = 0; int _cf = 0; int _flags = 0;
+    uint32_t _sahf_flags = 0; int _sahf_of = 0;
+    (void)_sahf_flags; (void)_sahf_of;
+    uint32_t _shift_result = 0, _rotate_result = 0; int _shift_of = 0;
+    (void)_shift_result; (void)_rotate_result; (void)_shift_of;
     uint32_t _fa = 0, _fb = 0; int32_t _fas = 0, _fbs = 0;
     (void)ebp; (void)_cf; (void)_flags;
     (void)_fa; (void)_fb; (void)_fas; (void)_fbs;
diff --git a/tools/conformance/xbe_run.py b/tools/conformance/xbe_run.py
index 7f55de0..662c5be 100644
--- a/tools/conformance/xbe_run.py
+++ b/tools/conformance/xbe_run.py
@@ -284,6 +284,7 @@ RECOMP_TLS uint32_t g_eax, g_ecx, g_edx, g_esp, g_ebx, g_esi, g_edi;
 RECOMP_TLS uint32_t g_seh_ebp, g_ebp;
 RECOMP_TLS double g_fp_stack[8]; RECOMP_TLS int g_fp_top;
 RECOMP_TLS uint16_t g_fp_control_word = 0x027F; RECOMP_TLS int g_fp_cmp;
+RECOMP_TLS uint16_t g_fp_status_word; RECOMP_TLS uint8_t g_fp_empty_mask = 0xFF;
 RECOMP_TLS RecompXmm g_xmm0,g_xmm1,g_xmm2,g_xmm3,g_xmm4,g_xmm5,g_xmm6,g_xmm7;
 volatile uint32_t g_icall_trace[16]; volatile uint32_t g_icall_trace_idx;
 volatile uint64_t g_icall_count;
@@ -579,10 +580,10 @@ static void run_one(uint32_t va, const uint32_t *raw, int vec, int *shown) {

     reset_memory();

-    g_fp_cmp = 0; g_fp_control_word = 0x027Fu;
+    g_fp_cmp = 0; g_fp_status_word = 0; g_fp_empty_mask = 0xFF; g_fp_control_word = 0x027Fu;
     memset(g_fp_stack, 0, sizeof g_fp_stack);
     /* one value on the x87 stack, matching the native side's fld */
-    g_fp_top = 7; g_fp_stack[7] = g_fp_arg;
+    g_fp_top = 7; g_fp_stack[7] = g_fp_arg; g_fp_empty_mask = 0x7F;
     g_eax = g_ecx = g_edx = g_ebx = g_esi = g_edi = 0;
     g_seh_ebp = g_ebp = 0;
     for (i = 3; i >= 0; i--) { sp -= 4; memcpy(sp, &args[i], 4); }
diff --git a/tools/recomp/__main__.py b/tools/recomp/__main__.py
index 55fec47..a1d3c6c 100644
--- a/tools/recomp/__main__.py
+++ b/tools/recomp/__main__.py
@@ -139,7 +139,7 @@ def _report_unimplemented(stats):
     if not unimplemented:
         return
     total = sum(len(a) for a in unimplemented.values())
-    print(f"{total} instruction(s) unimplemented, emitted as no-op comments "
+    print(f"{total} instruction(s) unimplemented, emitted as explicit execution boundaries "
           f"({len(unimplemented)} distinct mnemonics):", file=sys.stderr)
     ranked = sorted(unimplemented.items(),
                     key=lambda kv: (-len(kv[1]), kv[0]))
diff --git a/tools/recomp/lifter.py b/tools/recomp/lifter.py
index 468312d..d079a66 100644
--- a/tools/recomp/lifter.py
+++ b/tools/recomp/lifter.py
@@ -254,6 +254,7 @@ FLAG_SETTERS = frozenset({
 # this shape: "add reg, reg" to shift the top bit into CF, then jae on it.
 CF_TRACKED = frozenset({
     "add", "sub", "adc", "sbb", "shl", "shr", "sar",
+    "rol", "ror", "rcl", "rcr", "shld", "shrd",
 })

 # Additional instructions that modify EFLAGS (tracked but handled as generic)
@@ -333,6 +334,21 @@ _EFLAGS_PRESERVE = frozenset({
 })


+def _overflow_snapshot(state):
+    """Return a proven OF expression before SAHF, or None for unknown OF."""
+    if not state: return None
+    kind, ops = state
+    if kind == "sahf_of": return "_sahf_of"
+    if kind == "cmp" and len(ops) == 2:
+        bits = (_operand_width(ops[0]) or _operand_width(ops[1]) or 4) * 8
+        return f"!!(((_fa ^ _fb) & (_fa ^ (_fa - _fb))) & 0x{1 << (bits - 1):X}u)"
+    if kind == "test": return "0"
+    if kind in ("rol", "ror", "rcl", "rcr", "shl", "shr", "shld", "shrd"):
+        condition = _make_condition("jo", kind, ops)
+        return condition[0] if condition else None
+    return None
+
+
 def _make_condition(jcc, flag_setter, flag_ops):
     """
     Generate a C condition expression for a jcc based on what set the flags.
@@ -342,6 +358,15 @@ def _make_condition(jcc, flag_setter, flag_ops):
     if not cond_info:
         return None
     cmp_macro, test_macro, desc = cond_info
+    if flag_setter in ("snapshot_zf", "snapshot_sz"):
+        if jcc in ("je", "jz"): return "_zf", desc
+        if jcc in ("jne", "jnz"): return "!_zf", desc
+        if flag_setter == "snapshot_sz":
+            if jcc in ("jl", "jnge"): return "_slt", desc
+            if jcc in ("jge", "jnl"): return "!_slt", desc
+            if jcc in ("jle", "jng"): return "(_slt || _zf)", desc
+            if jcc in ("jg", "jnle"): return "(!_slt && !_zf)", desc
+        return None  # No CF, raw SF, or raw OF is established by this meet.

     # A cmp/test that is not fused with its jcc snapshots its operands into
     # _fa/_fb (zero-extended) and _fas/_fbs (sign-extended) at the point the
@@ -361,9 +386,28 @@ def _make_condition(jcc, flag_setter, flag_ops):
         lhs = None
         rhs = None

-    # ── FPU compare-to-EFLAGS and sahf: no standard operands ──
+    if flag_setter in ("sahf", "sahf_of"):
+        cf, zf, pf, sf = (f"!!(_sahf_flags & {mask}u)" for mask in (1, 64, 4, 128))
+        predicates = {
+            "jb": cf, "jnae": cf, "jc": cf,
+            "jae": f"!({cf})", "jnb": f"!({cf})", "jnc": f"!({cf})",
+            "jbe": f"({cf} || {zf})", "jna": f"({cf} || {zf})",
+            "ja": f"!({cf} || {zf})", "jnbe": f"!({cf} || {zf})",
+            "je": zf, "jz": zf, "jne": f"!({zf})", "jnz": f"!({zf})",
+            "jp": pf, "jnp": f"!({pf})", "js": sf, "jns": f"!({sf})",
+        }
+        if flag_setter == "sahf_of":
+            slt = f"({sf} != _sahf_of)"
+            predicates.update({"jo": "_sahf_of", "jno": "!_sahf_of",
+                "jl": slt, "jnge": slt, "jge": f"!{slt}", "jnl": f"!{slt}",
+                "jle": f"({slt} || {zf})", "jng": f"({slt} || {zf})",
+                "jg": f"!({slt} || {zf})", "jnle": f"!({slt} || {zf})"})
+        value = predicates.get(jcc)
+        return (value, desc) if value is not None else None
+
+    # ── FPU compare-to-EFLAGS: no standard operands ──
     if flag_setter in ("fcompi", "fcomip", "fucomi", "fucompi",
-                        "fucomip", "fcomi", "sahf"):
+                        "fucomip", "fcomi"):
         fpu_cmp_map = {
             "ja": ">", "jnbe": ">",
             "jae": ">=", "jnb": ">=", "jnc": ">=",
@@ -385,6 +429,19 @@ def _make_condition(jcc, flag_setter, flag_ops):
     if lhs is None:
         return None

+    if flag_setter in ("rol", "ror", "rcl", "rcr"):
+        if jcc in ("jb", "jnae", "jc"): return "_cf", desc
+        if jcc in ("jae", "jnb", "jnc"): return "!_cf", desc
+        if (jcc in ("jo", "jno") and len(flag_ops) > 1 and
+                flag_ops[1].type == "imm" and (flag_ops[1].imm & 31) == 1):
+            bits = (_operand_width(flag_ops[0]) or 4) * 8
+            other = ("_rotate_result" if flag_setter == "rol" else "_cf" if flag_setter == "rcl"
+                     else f"(_rotate_result >> {bits - 2})")
+            overflow = f"(((_rotate_result >> {bits - 1}) ^ {other}) & 1u)"
+            return (overflow if jcc == "jo" else f"!{overflow}"), desc
+        # Rotation never establishes ZF/SF/PF. A multi-bit OF is undefined.
+        return None
+
     # ── comiss/ucomiss: float comparison, sets CF/ZF/PF ──
     if flag_setter in ("comiss", "comisd", "ucomiss", "ucomisd"):
         def _sse_op(op):
@@ -485,6 +542,10 @@ def _make_condition(jcc, flag_setter, flag_ops):
             return f"(!RECOMP_PARITY8(({lhs}) & ({rhs})))", desc
         return None

+    if flag_setter in ("shl", "shr", "shld", "shrd"):
+        # Like CMP snapshots, this survives an intervening MOV to the operand.
+        lhs = "_shift_result"
+
     # ── carry conditions, from the _cf the arithmetic already produced ──
     #
     # Ahead of the per-mnemonic rules below, which reconstruct CF from the
@@ -630,27 +691,22 @@ def _make_condition(jcc, flag_setter, flag_ops):
         return None

     # ── shift: result-based ──
-    if flag_setter in ("shl", "shr", "sar"):
-        if jcc in ("je", "jz"):
-            return f"({lhs} == 0)", desc
-        if jcc in ("jne", "jnz"):
-            return f"({lhs} != 0)", desc
-        if jcc == "js":
-            return f"((int32_t){lhs} < 0)", desc
-        if jcc == "jns":
-            return f"((int32_t){lhs} >= 0)", desc
-        return None
-
-    # ── shld/shrd: double-precision shift, result-based ──
-    if flag_setter in ("shld", "shrd"):
+    if flag_setter in ("shl", "shr", "sar", "shld", "shrd"):
         if jcc in ("je", "jz"):
             return f"({lhs} == 0)", desc
         if jcc in ("jne", "jnz"):
             return f"({lhs} != 0)", desc
         if jcc == "js":
-            return f"((int32_t){lhs} < 0)", desc
+            return f"({_sf_cast}{lhs} < 0)", desc
         if jcc == "jns":
-            return f"((int32_t){lhs} >= 0)", desc
+            return f"({_sf_cast}{lhs} >= 0)", desc
+        if flag_setter in ("shl", "shr", "shld", "shrd"):
+            if jcc in ("jp", "jnp"):
+                condition = f"RECOMP_PARITY8({lhs})"
+                return (condition if jcc == "jp" else f"!{condition}"), desc
+            if (jcc in ("jo", "jno") and len(flag_ops) > 1 and
+                    flag_ops[-1].type == "imm" and (flag_ops[-1].imm & 31) == 1):
+                return ("_shift_of" if jcc == "jo" else "!_shift_of"), desc
         return None

     # ── rol/ror/rcl/rcr: rotation, only CF/OF affected ──
@@ -745,11 +801,11 @@ def _emit_cond_goto(cond_expr, jcc, desc, target, lifter):
         # Conditional tail call: same frame bridge as the unconditional tail
         # jmp in _lift_jmp, applied only on the taken path.
         if target in lifter.manual_functions:
-            return (f"if ({cond_expr}) {{ g_seh_ebp = ebp; "
+            return (f"if ({cond_expr}) {{ g_ebp = g_seh_ebp = ebp; "
                     f"RECOMP_ITAIL(0x{target:08X}u); return; }}"
                     f" /* {jcc}: {desc}, manual tail */")
         name = lifter._call_target_name(target)
-        return (f"if ({cond_expr}) {{ g_seh_ebp = ebp; {name}(); return; }}"
+        return (f"if ({cond_expr}) {{ g_ebp = g_seh_ebp = ebp; {name}(); return; }}"
                 f" /* {jcc}: {desc} */")
     return f"if ({cond_expr}) goto loc_{target:08X}; /* {jcc}: {desc} */"

@@ -939,7 +995,7 @@ class Lifter:
         self.func_start = 0  # Set per-function by translator
         self.func_end = 0
         self.needs_cf = False  # Set per-function by translator (has adc/sbb)
-        self.publishes_ebp = False  # Set per-function: has a real frame
+        self.publishes_ebp = False  # Set per-function: has a local architectural EBP
         self.trace_exit_name = None  # Set per-function when traced
         # Every direct call target we emit a name for, as {addr: name}. The
         # batch translator diffs this against the functions it actually defined
@@ -1045,6 +1101,8 @@ class Lifter:
             return self._lift_sar(insn, ops)
         if m in ("rol", "ror"):
             return self._lift_rotate(insn, ops, m)
+        if m in ("rcl", "rcr"):
+            return self._lift_carry_rotate(insn, ops, m)

         # ── Comparison / test (standalone, not part of cmp+jcc pattern) ──
         if m == "cmp":
@@ -1163,7 +1221,9 @@ class Lifter:
         if m == "lahf":
             return ["/* lahf - load AH from flags (used in FPU compare idiom) */"]
         if m == "sahf":
-            return ["/* sahf - store AH to flags */"]
+            out = ["_sahf_flags = (eax >> 8) & 0xD5u; /* SAHF snapshots AH; preserves OF */"]
+            if getattr(self, "needs_cf", False): out.append("_cf = _sahf_flags & 1u;")
+            return out
         if m == "shld":
             return self._lift_shld(insn, ops)
         if m == "shrd":
@@ -1172,12 +1232,9 @@ class Lifter:
             if len(ops) >= 2:
                 return [f"/* bt {_fmt_operand_read(ops[0])}, {_fmt_operand_read(ops[1])} - bit test */"]
             return [f"/* bt {insn.op_str} */"]
-        if m == "emms":
-            # A statement rather than a comment: emms genuinely has no
-            # effect here (mm/x87 aliasing is not modelled), and a
-            # comment-only lift is how the conformance suite reports a
-            # silently dropped instruction.
-            return ["(void)0; /* emms - empty MMX state */"]
+        if m in ("emms", "femms"):
+            # EMMS marks every physical x87 register empty without changing TOP.
+            return [f"g_fp_empty_mask = 0xFFu; /* {m}: all x87 registers empty */"]
         if m in ("xlat", "xlatb"):
             # AL indexes the byte table at EBX. With an address-size override,
             # the effective offset is calculated and wrapped at 16 bits.
@@ -1290,7 +1347,7 @@ class Lifter:
         if m in ("bsf", "bsr"):
             if len(ops) < 2:
                 self.unimplemented.setdefault(m, []).append(insn.address)
-                return [f"/* TODO: {m} {insn.op_str} */"]
+                return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]
             dst = _fmt_operand_read(ops[0])
             src = _fmt_operand_read(ops[1])
             width = (_operand_width(ops[1]) or 4) * 8
@@ -1359,11 +1416,14 @@ class Lifter:
         #
         # Recorded, not merely commented -- see self.unimplemented.
         self.unimplemented.setdefault(m, []).append(insn.address)
-        return [f"/* TODO: {m} {insn.op_str} */"]
+        return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]

     # ── MOV family ──

     def _lift_mov(self, insn, ops):
+        if any(op.type == "reg" and op.reg.startswith(("dr", "cr")) for op in ops):
+            self.unimplemented.setdefault(insn.mnemonic, []).append(insn.address)
+            return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* privileged mov */"]
         if nops := len(ops) < 2:
             return [f"/* mov: bad operands */"]
         src = _fmt_operand_read(ops[1])
@@ -1490,7 +1550,15 @@ class Lifter:
                 out.append(f"_cf = (int)((uint32_t)({dst}) < (uint32_t)({src}));")
             else:
                 out.append("_cf = 0; /* logical op clears CF */")
-        out.append(_fmt_operand_write(ops[0], expr))
+        if m == "sub" and getattr(self, "needs_zf_snapshot", False):
+            # Capture the executed result once, before a later MOV or join can
+            # change the destination. Do not re-read memory to reconstruct ZF.
+            mask = self._SNAP_MASK.get(_operand_width(ops[0]) or 4, "0xFFFFFFFFu")
+            out.extend([f"{{ uint32_t _zr = ((uint32_t)({dst}) - (uint32_t)({src})) & {mask};",
+                        "_zf = (_zr == 0);",
+                        _fmt_operand_write(ops[0], "_zr") + " }"])
+        else:
+            out.append(_fmt_operand_write(ops[0], expr))
         return out

     def _lift_inc_dec(self, insn, ops, m):
@@ -1499,6 +1567,11 @@ class Lifter:
         val = _fmt_operand_read(ops[0])
         delta = "1"
         op_char = "+" if m == "inc" else "-"
+        if m == "dec" and getattr(self, "needs_zf_snapshot", False):
+            mask = self._SNAP_MASK.get(_operand_width(ops[0]) or 4, "0xFFFFFFFFu")
+            return [f"{{ uint32_t _zr = ((uint32_t)({val}) - 1u) & {mask};",
+                    "_zf = (_zr == 0);",
+                    _fmt_operand_write(ops[0], "_zr") + " }"]
         # For sub-registers (al, cl, etc.), use the SET macro instead of ++
         if ops[0].type == "reg" and ops[0].reg in (
                 "eax", "ebx", "ecx", "edx", "esi", "edi", "ebp", "esp"):
@@ -1514,7 +1587,7 @@ class Lifter:
         if preserve_carry or self.needs_cf:
             # neg sets CF iff the operand was non-zero (neg/sbb sign-extract).
             out.append(f"_cf = (int)(({val}) != 0);")
-        out.append(_fmt_operand_write(ops[0], f"(uint32_t)(-(int32_t){val})"))
+        out.append(_fmt_operand_write(ops[0], f"(0u - (uint32_t)({val}))"))
         return out

     def _lift_not(self, insn, ops):
@@ -1549,24 +1622,25 @@ class Lifter:
                 % (dst, src, w, _fmt_operand_write(ops[0], "(uint32_t)_t"))]

     def _lift_shld(self, insn, ops):
-        """SHLD: double-precision shift left."""
-        if len(ops) < 3:
-            return [f"/* shld: bad operands */"]
-        dst = _fmt_operand_read(ops[0])
-        src = _fmt_operand_read(ops[1])
-        cnt = _fmt_operand_read(ops[2])
-        return [_fmt_operand_write(ops[0],
-            f"({dst} << {cnt}) | ({src} >> (32 - {cnt}))") + " /* shld */"]
+        return self._lift_double_shift(insn, ops, False)

     def _lift_shrd(self, insn, ops):
-        """SHRD: double-precision shift right."""
+        return self._lift_double_shift(insn, ops, True)
+
+    def _lift_double_shift(self, insn, ops, right):
         if len(ops) < 3:
-            return [f"/* shrd: bad operands */"]
-        dst = _fmt_operand_read(ops[0])
-        src = _fmt_operand_read(ops[1])
-        cnt = _fmt_operand_read(ops[2])
-        return [_fmt_operand_write(ops[0],
-            f"({dst} >> {cnt}) | ({src} << (32 - {cnt}))") + " /* shrd */"]
+            return ["/* double shift: bad operands */"]
+        if ops[2].type == "imm" and (ops[2].imm & 31) == 0:
+            return ["(void)0; /* double shift: masked zero count preserves flags/result */"]
+        dst, src, cnt = [_fmt_operand_read(op) for op in ops[:3]]
+        bits = (_operand_width(ops[0]) or 4) * 8
+        carry = "&_cf" if self.needs_cf else "NULL"
+        snapshot = getattr(self, "needs_shift_snapshot", False)
+        overflow = "&_shift_of" if snapshot else "NULL"
+        expression = f"RECOMP_DOUBLE_SHIFT({dst}, {src}, {cnt}, {bits}, {int(right)}, {carry}, {overflow})"
+        if snapshot:
+            return [f"_shift_result = {expression};", _fmt_operand_write(ops[0], "_shift_result")]
+        return [_fmt_operand_write(ops[0], expression)]

     def _lift_imul(self, insn, ops):
         nops = len(ops)
@@ -1614,36 +1688,53 @@ class Lifter:

     def _lift_shift(self, insn, ops, c_op):
         if len(ops) < 2:
-            return [f"/* shift: bad operands */"]
+            return ["/* shift: bad operands */"]
+        if ops[1].type == "imm" and (ops[1].imm & 31) == 0:
+            return ["(void)0; /* logical shift: masked zero count preserves flags/result */"]
         dst = _fmt_operand_read(ops[0])
         cnt = _fmt_operand_read(ops[1])
-        out = []
-        if self.needs_cf:
-            w = (_operand_width(ops[0]) or 4) * 8
-            # CF is the last bit shifted out; a zero count leaves CF alone.
-            bit = f"({cnt}) - 1" if c_op == ">>" else f"{w} - ({cnt})"
-            out.append(f"if ({cnt}) _cf = (int)((({dst}) >> ({bit})) & 1);")
-        out.append(_fmt_operand_write(ops[0], f"{dst} {c_op} {cnt}"))
-        return out
+        bits = (_operand_width(ops[0]) or 4) * 8
+        carry = "&_cf" if self.needs_cf else "NULL"
+        snapshot = getattr(self, "needs_shift_snapshot", False)
+        overflow = "&_shift_of" if snapshot else "NULL"
+        expression = f"RECOMP_SHIFT({dst}, {cnt}, {bits}, {int(c_op == '>>')}, {carry}, {overflow})"
+        if snapshot:
+            return [f"_shift_result = {expression};", _fmt_operand_write(ops[0], "_shift_result")]
+        return [_fmt_operand_write(ops[0], expression)]

     def _lift_sar(self, insn, ops):
         if len(ops) < 2:
             return ["/* sar: bad operands */"]
         dst = _fmt_operand_read(ops[0])
         cnt = _fmt_operand_read(ops[1])
-        out = []
-        if self.needs_cf:
-            out.append(f"if ({cnt}) _cf = (int)((({dst}) >> (({cnt}) - 1)) & 1);")
-        out.append(_fmt_operand_write(ops[0], f"(uint32_t)((int32_t){dst} >> {cnt})"))
-        return out
+        bits = (_operand_width(ops[0]) or 4) * 8
+        carry = "&_cf" if self.needs_cf else "NULL"
+        return [_fmt_operand_write(ops[0], f"RECOMP_SAR({dst}, {cnt}, {bits}, {carry})")]

     def _lift_rotate(self, insn, ops, m):
         if len(ops) < 2:
             return [f"/* {m}: bad operands */"]
         dst = _fmt_operand_read(ops[0])
         cnt = _fmt_operand_read(ops[1])
-        func = "ROL32" if m == "rol" else "ROR32"
-        return [_fmt_operand_write(ops[0], f"{func}({dst}, {cnt})")]
+        bits = (_operand_width(ops[0]) or 4) * 8
+        carry = "&_cf" if self.needs_cf else "NULL"
+        expression = f"RECOMP_ROTATE({dst}, {cnt}, {bits}, {int(m == 'ror')}, {carry}, NULL)"
+        if getattr(self, "needs_rotate_snapshot", False):
+            return [f"_rotate_result = {expression};", _fmt_operand_write(ops[0], "_rotate_result")]
+        return [_fmt_operand_write(ops[0], expression)]
+
+    def _lift_carry_rotate(self, insn, ops, m):
+        if len(ops) < 2:
+            return [f"/* {m}: bad operands */"]
+        if ops[1].type == "imm" and (ops[1].imm & 31) == 0:
+            return ["(void)0; /* carry rotate: masked zero count preserves flags/result */"]
+        dst = _fmt_operand_read(ops[0])
+        cnt = _fmt_operand_read(ops[1])
+        bits = (_operand_width(ops[0]) or 4) * 8
+        expression = f"RECOMP_CARRY_ROTATE({dst}, {cnt}, {bits}, {int(m == 'rcr')}, &_cf, NULL)"
+        if getattr(self, "needs_rotate_snapshot", False):
+            return [f"_rotate_result = {expression};", _fmt_operand_write(ops[0], "_rotate_result")]
+        return [_fmt_operand_write(ops[0], expression)]

     # ── Compare / Test (standalone) ──

@@ -1681,6 +1772,14 @@ class Lifter:
             f"_fas = (int32_t){sx}(_fa); _fbs = (int32_t){sx}(_fb);"
             f" /* {kind} {lhs}, {rhs} ({size*8}-bit) */",
         ]
+        if getattr(self, "needs_zf_snapshot", False):
+            out.append("_zf = (_fa == _fb);" if kind == "cmp"
+                       else "_zf = ((_fa & _fb) == 0);")
+        if getattr(self, "needs_signed_snapshot", False):
+            # Signed-less is SF xor OF, not SF alone (CMP may overflow).
+            # TEST clears OF and takes SF from its width-masked AND result.
+            out.append("_slt = (_fas < _fbs);" if kind == "cmp"
+                       else f"_slt = ((int32_t){sx}(_fa & _fb) < 0);")
         # A cmp sets the carry flag too, and sbb/adc/setc/rcl read it directly
         # rather than through _fa/_fb. Leaving CF alone here let those pick up
         # whatever an earlier instruction had left in it.
@@ -1759,14 +1858,8 @@ class Lifter:
         if insn.call_target:
             name = self._call_target_name(insn.call_target)
             lines = []
-            # Re-publish this function's frame before every call, not just once
-            # at `mov ebp, esp`. g_ebp is "the last frame established anywhere",
-            # so a callee that sets up its own frame overwrites it and leaves it
-            # stale on return. A frameless helper called afterwards then
-            # inherits the wrong frame -- in Halo, sub_001E1BA0 called one
-            # function, returned, then called the frameless sub_001DEC07, which
-            # inherited a long-dead frame of ~0xA6 and wrote [ebp-0xa2] and
-            # [ebp-0xa0] onto Xbox VA 4 and 6: exactly the fs:[4] corruption.
+            # Publish the actual local register, including nonstandard EBP
+            # arguments, before crossing a compiled-function boundary.
             if self.publishes_ebp:
                 lines.append("g_ebp = ebp; /* frame stays current across calls */")
                 lines.append("g_seh_ebp = ebp;")
@@ -1856,7 +1949,8 @@ class Lifter:
             # target -- `call dword ptr [esp+8]`, the shape MSVC gives a
             # callback invoked through a stack argument -- is read four bytes
             # low and dispatches through the wrong slot.
-            return [f"{{ uint32_t _icall_target = {target}; "
+            prefix = "g_ebp = g_seh_ebp = ebp; " if self.publishes_ebp else ""
+            return [f"{prefix}{{ uint32_t _icall_target = {target}; "
                     f"PUSH32(esp, 0x{ret_va:08X}u); "
                     "RECOMP_ICALL_SAFE(_icall_target, _icall_esp); }"
                     " /* indirect call */"]
@@ -1865,11 +1959,13 @@ class Lifter:
     def _lift_ret(self, insn, ops):
         # x86 'ret' pops return address from stack.
         # 'ret N' also pops N extra bytes (stdcall cleanup).
-        # If this function IS __SEH_prolog or __SEH_epilog, bridge ebp
-        # so the caller can read back the frame pointer.
+        # Publish the register after the original POP/LEAVE epilog. Relays
+        # without a local EBP then inherit the restored frame, not a dead child.
+        # SEH helpers deliberately return a changed frame; keep that exchange.
         prefix = ""
-        if self.func_start in (self.SEH_PROLOG, self.SEH_EPILOG):
-            prefix = "g_seh_ebp = ebp; "
+        if (self.publishes_ebp
+                or self.func_start in (self.SEH_PROLOG, self.SEH_EPILOG)):
+            prefix = "g_ebp = g_seh_ebp = ebp; "
         # Exit trace, for functions that return with a register the caller
         # relied on holding something else. Entry tracing alone cannot show
         # that: it tells you what went in, never what came back. Emitted at the
@@ -1958,7 +2054,7 @@ class Lifter:
                 # Bridge ebp so the target function can inherit our frame pointer.
                 if insn.jump_target in self.manual_functions:
                     tail = (
-                        f"g_seh_ebp = ebp; "
+                        f"g_ebp = g_seh_ebp = ebp; "
                         f"RECOMP_ITAIL(0x{insn.jump_target:08X}u); return; "
                         f"/* manual tail jmp 0x{insn.jump_target:08X} */"
                     )
@@ -1970,7 +2066,7 @@ class Lifter:
                         ]
                     return [tail]
                 name = self._call_target_name(insn.jump_target)
-                tail = (f"g_seh_ebp = ebp; {name}(); return; "
+                tail = (f"g_ebp = g_seh_ebp = ebp; {name}(); return; "
                         f"/* tail jmp 0x{insn.jump_target:08X} */")
                 if self.trace_exit_name:
                     # Tag the exit so a trace says which one was taken. A
@@ -1990,10 +2086,10 @@ class Lifter:
                 lines = [f"{{ uint32_t _jt = {target_expr}; /* switch: {len(switch_targets)} entries, {len(unique_targets)} targets */"]
                 for t in unique_targets:
                     lines.append(f"if (_jt == 0x{t:08X}u) goto loc_{t:08X};")
-                lines.append(f"g_seh_ebp = ebp; RECOMP_ITAIL(_jt); return; }}")
+                lines.append(f"g_ebp = g_seh_ebp = ebp; RECOMP_ITAIL(_jt); return; }}")
                 return lines
             target = _fmt_operand_read(ops[0])
-            return [f"g_seh_ebp = ebp; RECOMP_ITAIL({target}); return; /* indirect tail jmp */"]
+            return [f"g_ebp = g_seh_ebp = ebp; RECOMP_ITAIL({target}); return; /* indirect tail jmp */"]
         return ["/* jmp: no target */"]

     def _lift_jcc(self, insn):
@@ -2007,7 +2103,7 @@ class Lifter:
             if target:
                 if self._is_external_target(target):
                     name = self._call_target_name(target)
-                    return [f"if ({cond}) {{ g_seh_ebp = ebp; {name}(); return; }} /* {jcc} */"]
+                    return [f"if ({cond}) {{ g_ebp = g_seh_ebp = ebp; {name}(); return; }} /* {jcc} */"]
                 return [f"if ({cond}) goto loc_{target:08X}; /* {jcc} */"]
             return [f"/* {jcc} - no target */"]

@@ -2030,7 +2126,7 @@ class Lifter:
         if target:
             if self._is_external_target(target):
                 name = self._call_target_name(target)
-                return [f"if ({cond} /* {jcc}: {desc} */) {{ g_seh_ebp = ebp; {name}(); return; }}"]
+                return [f"if ({cond} /* {jcc}: {desc} */) {{ g_ebp = g_seh_ebp = ebp; {name}(); return; }}"]
             return [f"if ({cond} /* {jcc}: {desc} */) goto loc_{target:08X};"]
         return [f"/* {jcc}: {desc} - no target */"]

@@ -2263,21 +2359,13 @@ class Lifter:
         if not ops:
             return [f"/* {m}: no operands */"]

-        # emms/femms: only the x87 tag word, which is not modelled.
-        if m in ("emms", "femms"):
-            # A real statement, not a comment: this instruction genuinely has
-            # no effect in this model, and the conformance suite treats a
-            # comment-only lift as a silently dropped instruction -- which is
-            # exactly the signal that must stay meaningful for the ones that
-            # really are missing.
-            return [f"(void)0; /* {m}: mm/x87 aliasing is not modelled */"]

         dst = ops[0]

         if m in self._MMX_BINARY and len(ops) >= 2:
             a, b = src(dst), src(ops[1])
             if a is None or b is None:
-                return [f"/* TODO: {m} {insn.op_str} */"]
+                return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]
             if is_mm(dst):
                 return [f"{dst.reg} = {self._MMX_BINARY[m]}({a}, {b}); /* {m} */"]
             return [f"/* TODO: {m} {insn.op_str} (dst not mm) */"]
@@ -2291,7 +2379,7 @@ class Lifter:
             elif count.type == "mem":
                 cnt = f"MMX_MEM({_fmt_mem(count)}).q"
             else:
-                return [f"/* TODO: {m} {insn.op_str} */"]
+                return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]
             return [f"{dst.reg} = {self._MMX_SHIFT[m]}({dst.reg}, {cnt}); /* {m} */"]

         # cvtps2pi / cvttps2pi: two singles in, two dwords out. The source is
@@ -2306,20 +2394,20 @@ class Lifter:
                 addr = _fmt_mem(s_op)
                 lo, hi = f"MEMF({addr})", f"MEMF(({addr}) + 4)"
             else:
-                return [f"/* TODO: {m} {insn.op_str} */"]
+                return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]
             return [f"{dst.reg} = MMX_FROM_PS({lo}, {hi}, {trunc}); /* {m} */"]

         if m == "pshufw" and len(ops) >= 3 and is_mm(dst):
             a = src(ops[1])
             if a is None:
-                return [f"/* TODO: {m} {insn.op_str} */"]
+                return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]
             return [f"{dst.reg} = MMX_PSHUFW({a}, {ops[2].imm & 0xFF}u); /* pshufw */"]

         if m == "pinsrw" and len(ops) >= 3 and is_mm(dst):
             v = (f"{ops[1].reg}" if ops[1].type == "reg"
                  else f"MEM16({_fmt_mem(ops[1])})" if ops[1].type == "mem" else None)
             if v is None:
-                return [f"/* TODO: {m} {insn.op_str} */"]
+                return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]
             return [f"{dst.reg} = MMX_PINSRW({dst.reg}, {v}, "
                     f"{ops[2].imm & 0xFF}u); /* pinsrw */"]

@@ -2351,14 +2439,14 @@ class Lifter:
                              " /* movd */"])
                 if dst.type == "reg":
                     return [f"{dst.reg} = {ops[1].reg}.ud[0]; /* movd */"]
-            return [f"/* TODO: {m} {insn.op_str} */"]
+            return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]

         # movntq: a non-temporal store. The hint is irrelevant; the store is not.
         if m == "movntq" and len(ops) >= 2 and dst.type == "mem" and is_mm(ops[1]):
             return [f"MMX_STORE({_fmt_mem(dst)}, {ops[1].reg}); /* movntq */"]

         self.unimplemented.setdefault(m, []).append(insn.address)
-        return [f"/* TODO: {m} {insn.op_str} */"]
+        return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: {m} {insn.op_str} */"]

     def _lift_sse(self, insn, m, ops):
         """Translate SSE instructions to C float operations."""
@@ -2758,6 +2846,10 @@ class Lifter:
                     dst = "fp_st1()" if idx == 1 else \
                           f"g_fp_stack[(g_fp_top + {idx}) & 7]"
                     parts.append(f"{dst} = fp_top();")
+                    parts.append(f"g_fp_empty_mask = (uint8_t)((g_fp_empty_mask & "
+                                 f"~(1u << ((g_fp_top + {idx}) & 7u))) | "
+                                 f"(((g_fp_empty_mask >> g_fp_top) & 1u) << "
+                                 f"((g_fp_top + {idx}) & 7u)));")
                 if m == "fstp":
                     parts.append("fp_pop();")
                 body = " ".join(parts) if parts else "(void)0;"
@@ -2776,9 +2868,32 @@ class Lifter:
                 mem_acc = _smem_accessor(size)
                 int_type = {2: "int16_t", 4: "int32_t", 8: "int64_t"}.get(
                     size, "int32_t")
+                bits = {2: 16, 4: 32, 8: 64}.get(size, 32)
                 pop = " fp_pop();" if m == "fistp" else ""
-                return [f"{mem_acc}({_fmt_mem(ops[0])}) = "
-                        f"({int_type})llrint(fp_top());{pop} /* {m} */"]
+                # FLDCW changes the guest rounding control, not the host's
+                # floating-point environment. D3DX explicitly selects truncate
+                # before FISTP(value*255 + .5); host llrint rounded white to 256
+                # and corrupted the compressed texture's color/alpha bytes.
+                # Implement nearest-even independently of host rounding too.
+                return [
+                    "{ double _fist_value = fp_top(), _fist_rounded;",
+                    "  switch ((g_fp_control_word >> 10) & 3u) {",
+                    "  case 1: _fist_rounded = floor(_fist_value); break;",
+                    "  case 2: _fist_rounded = ceil(_fist_value); break;",
+                    "  case 3: _fist_rounded = trunc(_fist_value); break;",
+                    "  default: { double _fist_floor = floor(_fist_value);",
+                    "    double _fist_fraction = _fist_value - _fist_floor;",
+                    "    _fist_rounded = _fist_floor + "
+                    "(_fist_fraction > 0.5 || (_fist_fraction == 0.5 && "
+                    "fmod(_fist_floor, 2.0) != 0.0)); break; }",
+                    "  }",
+                    # Masked invalid conversions store the x87 integer
+                    # indefinite value; avoid undefined out-of-range C casts.
+                    f"  {mem_acc}({_fmt_mem(ops[0])}) = "
+                    f"(!isfinite(_fist_rounded) || _fist_rounded < -0x1p{bits - 1} "
+                    f"|| _fist_rounded >= 0x1p{bits - 1}) ? INT{bits}_MIN : "
+                    f"({int_type})_fist_rounded;{pop} }} /* {m}: guest x87 RC */",
+                ]
             return [f"/* {m} {insn.op_str} */"]

         if m in ("fadd", "faddp", "fsub", "fsubp", "fsubr", "fsubrp",
@@ -2895,15 +3010,29 @@ class Lifter:
         if m == "f2xm1":
             return [f"fp_top() = exp2(fp_top()) - 1.0; /* f2xm1 */"]
         if m in ("fprem", "fprem1"):
-            # Both leave the remainder in st0 and clear C2 to say "complete".
-            # fprem truncates toward zero, fprem1 rounds to nearest (IEEE), which
-            # is the difference between fmod and remainder.
-            fn = "fmod" if m == "fprem" else "remainder"
-            return [f"fp_top() = {fn}(fp_top(), fp_st1()); /* {m} */"]
+            return [f"fp_top() = recomp_fp_remainder(fp_top(), fp_st1(), {int(m == 'fprem1')}, 0x{insn.address:08X}u); /* {m} */"]
         if m == "fscale":
             return [f"fp_top() = ldexp(fp_top(), (int)fp_st1()); /* fscale */"]
         if m == "frndint":
-            return [f"fp_top() = rint(fp_top()); /* frndint */"]
+            # FLDCW only updates the guest control word. Host rint therefore
+            # follows an unrelated rounding mode. Every finite binary64 value
+            # at or above2^52 is already integral; preserve NaNs/Inf and zeros.
+            return [
+                "{ double _round_value = fp_top(), _round_result = _round_value;",
+                "  if (isfinite(_round_value) && fabs(_round_value) < 0x1p52) {",
+                "    switch ((g_fp_control_word >> 10) & 3u) {",
+                "    case 1: _round_result = floor(_round_value); break;",
+                "    case 2: _round_result = ceil(_round_value); break;",
+                "    case 3: _round_result = trunc(_round_value); break;",
+                "    default: { double _round_floor = floor(_round_value);",
+                "      double _round_fraction = _round_value - _round_floor;",
+                "      _round_result = _round_floor + (_round_fraction > 0.5 ||",
+                "        (_round_fraction == 0.5 && fmod(_round_floor, 2.0) != 0.0)); break; }",
+                "    }",
+                "    _round_result = copysign(_round_result, _round_value);",
+                "  }",
+                "  fp_top() = _round_result; } /* frndint: guest x87 RC */",
+            ]
         if m == "fldpi":
             return [f"fp_push(3.14159265358979323846); /* fldpi */"]
         if m == "fldl2e":
@@ -2915,8 +3044,15 @@ class Lifter:
         if m == "fldln2":
             return [f"fp_push(0.69314718055994530942); /* fldln2 */"]
         if m == "ftst":
-            return [f"g_fp_cmp = (fp_top() < 0.0) ? -1 : "
-                    f"(fp_top() > 0.0) ? 1 : 0; /* ftst */"]
+            return ["g_fp_cmp = RECOMP_FCMP(fp_top(), 0.0); "
+                    "RECOMP_FP_COMPARE_STATUS(); /* ftst */"]
+        if m == "fxam":
+            return ["recomp_fp_examine(); /* fxam: class/sign/empty at current TOP */"]
+        if m in ("fnclex", "fclex"):
+            return ["g_fp_status_word &= (uint16_t)~0x80FFu; /* fnclex: preserve TOP/condition codes */"]
+        if m in ("fninit", "finit"):
+            return ["g_fp_control_word = 0x037Fu; g_fp_status_word = 0; "
+                    "g_fp_cmp = 0; g_fp_top = 0; g_fp_empty_mask = 0xFFu; /* fninit */"]
         if m == "fxch":
             # fxch st(i) swaps st0 with st(i); the bare form is st(1). Was
             # hardcoded to st1, so fxch st(2)/st(3)/st(4) (86/7/1 sites in Halo)
@@ -2930,7 +3066,12 @@ class Lifter:
             i = (self._st_index(ops[-1].reg)
                  if (ops and ops[-1].type == "reg" and ops[-1].reg) else 1)
             dst = self._st_expr(i)
-            return [f"{{ double _t = fp_top(); fp_top() = {dst}; {dst} = _t; }}"
+            return [f"{{ double _t = fp_top(); fp_top() = {dst}; {dst} = _t; "
+                    f"unsigned _other = (g_fp_top + {i}) & 7u; "
+                    f"unsigned _different = ((g_fp_empty_mask >> g_fp_top) ^ "
+                    f"(g_fp_empty_mask >> _other)) & 1u; "
+                    f"g_fp_empty_mask ^= (uint8_t)((_different << g_fp_top) | "
+                    f"(_different << _other)); }}"
                     f" /* fxch {insn.op_str} */"]
         if m in ("fcom", "fcomp", "fcompp", "fucom", "fucomp", "fucompp"):
             # Compare st0 against the operand, not always st1. `fcomp [mem]`
@@ -2950,7 +3091,7 @@ class Lifter:
             npop = 2 if m.endswith("pp") else (1 if m.endswith("p") else 0)
             pops = " fp_pop();" * npop
             return [f"g_fp_cmp = RECOMP_FCMP(fp_top(), {rhs});"
-                    f"{pops} /* {m} {insn.op_str} */"]
+                    f" RECOMP_FP_COMPARE_STATUS();{pops} /* {m} {insn.op_str} */"]
         if m in ("fcompi", "fcomip", "fucomi", "fucompi", "fucomip", "fcomi"):
             # These set EFLAGS directly (CF, ZF, PF) from FPU comparison
             # fcompi/fucompi pop st(0) after comparing; fcomi/fucomi do not
@@ -2976,10 +3117,7 @@ class Lifter:
             # `test ah, 0x44; jp` -- the standard isnan idiom -- reads exactly
             # those two bits. Reporting equal for a NaN compare sends every
             # float classification in a title down the wrong branch.
-            status = ("(uint16_t)(((g_fp_top & 7u) << 11) |"
-                      " (g_fp_cmp == 2 ? 0x4500u :"
-                      " g_fp_cmp < 0 ? 0x0100u :"
-                      " g_fp_cmp > 0 ? 0x0000u : 0x4000u))")
+            status = "RECOMP_FP_STATUS()"
             if insn.op_str.strip() in ("ax", "eax"):
                 # `fnstsw ax` writes the whole of AX, not just AH.
                 return [f"eax = (eax & 0xFFFF0000u) | (uint32_t){status};"
@@ -3015,7 +3153,96 @@ class Lifter:
         if m == "fld1":
             return [f"fp_push(1.0); /* fld1 */"]

-        return [f"/* FPU: {m} {insn.op_str} */"]
+        # Save/restore requires the original 108-byte x87 environment/register
+        # image, including exception/tag state not represented by this model.
+        # Preserve an explicit execution boundary instead of dropping the op.
+        self.unimplemented.setdefault(m, []).append(insn.address)
+        return [f"recomp_unsupported_instruction(0x{insn.address:08X}u); /* TODO: x87 {m} {insn.op_str} */"]
+
+
+
+def merge_flag_states(states, allow_zf=True):
+    """Meet known predecessor states without discarding compatible snapshots.
+
+    CMP/TEST already write runtime operand snapshots on every incoming path.
+    The operands' identities therefore need not agree, only operation and width
+    (SF conditions still select a width-specific cast). Different CMP/TEST
+    semantics meet at captured ZF and signed-less (SF xor OF). DEC/SUB only
+    supply captured result ZF here; no preserved carry is inferred. Unknown
+    states and other unlike setters that re-read operands are not merged.
+    """
+    if not states or any(state is None for state in states):
+        return None
+    first = states[0]
+    if all(state == first for state in states[1:]):
+        return first
+
+    if all(state[0] in ("sahf", "sahf_of") for state in states):
+        return ("sahf", [])  # Every path supplies AH flags, but not necessarily OF.
+
+    def snapshot_signature(state):
+        kind, ops = state
+        if kind not in ("cmp", "test") or len(ops) != 2:
+            return None
+        width = _operand_width(ops[0]) or _operand_width(ops[1]) or 4
+        return kind, width
+
+    signature = snapshot_signature(first)
+    if signature is not None and all(snapshot_signature(state) == signature
+                                     for state in states[1:]):
+        return first
+    # Capabilities intersect at a join. Existing same-kind operand snapshots
+    # remain valid above; mixed CMP/TEST can also supply signed comparisons.
+    if allow_zf and all(state[0] == "snapshot_sz" or snapshot_signature(state) is not None
+                        for state in states):
+        return ("snapshot_sz", [])
+    def supplies_zf(state):
+        kind, ops = state
+        return (kind in ("snapshot_zf", "snapshot_sz") or
+                snapshot_signature(state) is not None or
+                (kind == "dec" and len(ops) == 1) or
+                (kind == "sub" and len(ops) == 2))
+    if allow_zf and all(supplies_zf(state) for state in states):
+        return ("snapshot_zf", [])
+    return None
+
+
+def advance_flag_state(curr, state):
+    """Pure flag transfer shared by CFG analysis and instruction emission."""
+    m = curr.mnemonic
+    if (m in ("rol", "ror", "rcl", "rcr", "sar", "shl", "shr") and len(curr.operands) > 1 and
+            curr.operands[1].type == "imm" and (curr.operands[1].imm & 31) == 0):
+        return state
+    if (m in ("shld", "shrd") and len(curr.operands) > 2 and
+            curr.operands[2].type == "imm" and (curr.operands[2].imm & 31) == 0):
+        return state
+    if m == "sahf":
+        return ("sahf_of" if _overflow_snapshot(state) is not None else "sahf", [])
+    if m in FLAG_SETTERS: return (m, list(curr.operands))
+    if m in _FLAGS_UNDEFINED: return None
+    if m in _EFLAGS_SETTERS: return (m, list(curr.operands))
+    if m in _EFLAGS_PRESERVE: return state
+    if m in ("fcompi", "fcomip", "fucomi", "fucompi", "fucomip", "fcomi", "sahf"):
+        return (m, list(curr.operands))
+    if m.startswith(("f", "cmov", "j", "set")): return state
+    if m.startswith("rep"):
+        rest = getattr(curr, "op_str", "")
+        if any(x in m or x in rest for x in ("cmpsb", "scasb")):
+            return (m, list(curr.operands))
+        return state
+    return None
+
+
+def block_flag_state(bb, incoming=None, conservative_calls=True):
+    state = incoming
+    for insn in bb.instructions:
+        # New cross-block proofs cannot assume an arbitrary callee preserves
+        # flags. Local emission/legacy state remain a separately staged policy.
+        if conservative_calls and (insn.is_call or insn.mnemonic == "popfd"):
+            state = None
+        else:
+            state = advance_flag_state(insn, state)
+    return state


 def lift_basic_block(lifter, bb, flag_state=None):
@@ -3131,56 +3358,17 @@ def lift_basic_block(lifter, bb, flag_state=None):
             results = lifter._lift_neg(
                 curr, curr.operands, preserve_carry=preserve)
         else:
+            if curr.mnemonic == "sahf":
+                overflow = _overflow_snapshot((last_flag_setter, last_flag_ops)
+                                              if last_flag_setter else None)
+                if overflow is not None:
+                    stmts.append(f"_sahf_of = {overflow}; /* OF survives SAHF */")
             results = lifter.lift_instruction(insns[i])
         stmts.extend(results)

-        # Track flag-setting instructions
-        if curr.mnemonic in FLAG_SETTERS:
-            last_flag_setter = curr.mnemonic
-            last_flag_ops = list(curr.operands)
-        elif curr.mnemonic in _FLAGS_UNDEFINED:
-            # Flags are undefined after these - clear tracking
-            last_flag_setter = None
-            last_flag_ops = []
-        elif curr.mnemonic in _EFLAGS_SETTERS:
-            # Additional flag-setting instructions
-            last_flag_setter = curr.mnemonic
-            last_flag_ops = list(curr.operands)
-        elif curr.mnemonic in _EFLAGS_PRESERVE:
-            pass  # These don't affect EFLAGS
-        elif curr.mnemonic in ("fcompi", "fcomip", "fucomi", "fucompi",
-                                "fucomip", "fcomi"):
-            # FPU compare-to-EFLAGS: sets CF, ZF, PF directly
-            last_flag_setter = curr.mnemonic
-            last_flag_ops = list(curr.operands)
-        elif curr.mnemonic == "sahf":
-            # sahf loads AH into flags - typically after fnstsw ax
-            # in the fcomp/fnstsw/sahf pattern for FPU comparisons
-            last_flag_setter = "sahf"
-            last_flag_ops = list(curr.operands)
-        elif curr.mnemonic.startswith("f") or curr.mnemonic.startswith("cmov"):
-            pass  # FPU and already-handled CMOVcc
-        elif curr.mnemonic.startswith("j"):
-            pass  # Jumps don't set flags
-        elif curr.mnemonic.startswith("set"):
-            pass  # SETcc doesn't set flags
-        elif curr.mnemonic.startswith("rep"):
-            # rep movsb/movsd = data copy, preserves flags
-            # repe cmpsb/repne scasb = comparison, sets flags
-            rest = curr.op_str.strip() if hasattr(curr, 'op_str') else ""
-            raw_m = curr.mnemonic
-            if "cmpsb" in raw_m or "scasb" in raw_m:
-                last_flag_setter = raw_m
-                last_flag_ops = list(curr.operands)
-            elif "cmpsb" in rest or "scasb" in rest:
-                last_flag_setter = raw_m
-                last_flag_ops = list(curr.operands)
-            else:
-                pass  # rep movs/stos = data movement, flags preserved
-        else:
-            # Unknown instruction - conservatively clear flag state
-            last_flag_setter = None
-            last_flag_ops = []
+        state = advance_flag_state(curr, (last_flag_setter, last_flag_ops)
+                                   if last_flag_setter else None)
+        last_flag_setter, last_flag_ops = state if state else (None, [])

         i += 1

diff --git a/tools/recomp/translator.py b/tools/recomp/translator.py
index 978b319..149e2a1 100644
--- a/tools/recomp/translator.py
+++ b/tools/recomp/translator.py
@@ -22,7 +22,7 @@ import struct
 from .config import va_to_file_offset, is_code_address
 from . import config as _config
 from .disasm import Disassembler
-from .lifter import (Lifter, lift_basic_block, detect_seh_helpers,
+from .lifter import (Lifter, lift_basic_block, merge_flag_states, block_flag_state, detect_seh_helpers,
                      detect_setjmp_helpers)


@@ -138,8 +138,10 @@ def _fixup_icall_esp_save(lines):
 FP_STACK_MACROS = [
     "    #define fp_push(v) do { double _fp_value = (v); \\",
     "        g_fp_top = (g_fp_top + 7u) & 7u; \\",
+    "        g_fp_empty_mask &= (uint8_t)~(1u << g_fp_top); \\",
     "        g_fp_stack[g_fp_top] = _fp_value; } while (0)",
-    "    #define fp_pop() (g_fp_top = (g_fp_top + 1u) & 7u)",
+    "    #define fp_pop() (g_fp_empty_mask |= (uint8_t)(1u << g_fp_top), \\",
+    "                      g_fp_top = (g_fp_top + 1u) & 7u)",
     "    #define fp_top() g_fp_stack[g_fp_top]",
     "    #define fp_st(i) g_fp_stack[(g_fp_top + (i)) & 7u]",
     "    #define fp_st1() fp_st(1)",
@@ -537,7 +539,7 @@ class FunctionTranslator:
         last_setter = None
         for insn in instructions:
             m = insn.mnemonic
-            if m in ("adc", "sbb", "stc", "clc", "cmc"):
+            if m in ("adc", "sbb", "stc", "clc", "cmc", "rcl", "rcr"):
                 return True
             cc = None
             if m.startswith("j") and len(m) > 1:
@@ -774,30 +776,10 @@ class FunctionTranslator:
         # and must NOT be declared locally, otherwise the local shadows
         # the global and cross-function register passing breaks.
         # Volatile registers (eax, ecx, edx, esp) are also global via macros.
-        reg_decls = []
+        # Even a standard prologue first PUSHes the incoming architectural EBP.
+        # Initializing only frameless functions corrupts saved guest frame bytes.
         if "ebp" in used_regs:
-            reg_decls.append("ebp")
-        if reg_decls:
-            lines.append(f"    uint32_t {', '.join(reg_decls)};")
-
-        # A function with no `push ebp; mov ebp, esp` prologue that still reads
-        # ebp is addressing its *caller's* frame. MSVC emits these for shared
-        # tails and helpers; Halo's CRT float formatting (sub_001DEC07) opens
-        # with `cmp byte ptr [edx+0xe], 5` and goes straight to [ebp-0xa4].
-        #
-        # ebp is a per-function local, so without this it starts as garbage and
-        # every [ebp-N] store lands wherever that points. In Halo that was the
-        # fake TIB at Xbox VA 0: `mov [ebp-0xa2], bx` destroyed fs:[4], and the
-        # next TLS lookup faulted, thousands of calls away from the cause.
-        #
-        # Inherit it instead. Frame-establishing functions publish g_ebp when
-        # they execute `mov ebp, esp` (see the lifter), so the value is the
-        # nearest enclosing frame -- which is exactly what the hardware ebp
-        # would still hold. Deliberately not the same as making ebp global:
-        # that also changes save/restore, and a callee that fails to restore
-        # then corrupts its caller (tried; esp underflowed inside XapiStartup).
-        if "ebp" in used_regs and not self._func_has_prologue(instructions):
-            lines.append("    ebp = g_ebp;  /* frameless: caller's frame */")
+            lines.append("    uint32_t ebp = g_ebp;")

         # Add _flags variable if function has conditional instructions
         has_conditionals = any(
@@ -851,6 +833,9 @@ class FunctionTranslator:
         # setter a branch reads is the lifter's tracking rule, mirrored here so
         # only the functions that consume CF declare it: computing it beside
         # every add in the image would be a line per add in 48,000 functions.
+        if any(insn.mnemonic == "sahf" for insn in instructions):
+            lines.append("    uint32_t _sahf_flags = 0; int _sahf_of = 0;")
+            lines.append("    (void)_sahf_flags; (void)_sahf_of;")
         has_carry = self._function_needs_cf(instructions)
         if has_carry:
             lines.append(f"    int _cf = 0; /* carry flag */")
@@ -859,7 +844,14 @@ class FunctionTranslator:
         # corrupts multi-word arithmetic (add/adc pairs) and the shr/adc
         # idiom MSVC emits for odd trailing elements.
         self.lifter.needs_cf = has_carry
-        self.lifter.publishes_ebp = self._func_owns_a_frame(instructions)
+        self.lifter.needs_shift_snapshot = any(insn.mnemonic in ("shl", "shr", "shld", "shrd") for insn in instructions)
+        if self.lifter.needs_shift_snapshot:
+            lines.append("    uint32_t _shift_result = 0; (void)_shift_result;")
+            lines.append("    int _shift_of = 0; (void)_shift_of;")
+        self.lifter.needs_rotate_snapshot = any(insn.mnemonic in ("rol", "ror", "rcl", "rcr") for insn in instructions)
+        if self.lifter.needs_rotate_snapshot:
+            lines.append("    uint32_t _rotate_result = 0; (void)_rotate_result;")
+        self.lifter.publishes_ebp = "ebp" in used_regs

         # SSE and MMX are architectural state, declared globally by the
         # runtime exactly like the GPRs and the x87 stack. Declaring either
@@ -914,19 +906,65 @@ class FunctionTranslator:
         # whatever instruction happens to sit above it -- silently, and with a
         # perfectly plausible-looking condition.
         preds = {bb.start: set() for bb in blocks}
+        legacy_preds = {bb.start: set() for bb in blocks}
         for i, bb in enumerate(blocks):
             last = bb.instructions[-1] if bb.instructions else None
             if last is None:
                 continue
             if last.jump_target in preds:
                 preds[last.jump_target].add(bb.start)
+                legacy_preds[last.jump_target].add(bb.start)
+            elif last.mnemonic == "jmp" and last.operands:
+                for destination in self.lifter._analyze_switch_table(last.operands):
+                    if destination in preds:
+                        preds[destination].add(bb.start)
             # A conditional jump also falls through; ret and an unconditional
             # jmp do not.
             leaves = last.is_ret or last.mnemonic in ("jmp", "int3", "ud2", "hlt")
             if not leaves and i + 1 < len(blocks):
                 preds[blocks[i + 1].start].add(bb.start)
+                legacy_preds[blocks[i + 1].start].add(bb.start)
+
+        # Establish terminal setters independently of address order. Propagate
+        # preserving blocks to a fixed point; unknown entry/call paths stay unknown.
+        out_state = {bb.start: block_flag_state(bb) for bb in blocks}
+        incoming_state = {}
+        for _ in range(len(blocks) + 1):
+            changed = False
+            for bb in blocks:
+                sources = preds[bb.start]
+                incoming = (merge_flag_states([out_state[p] for p in sorted(sources)])
+                            if bb.start != start and sources else None)
+                incoming_state[bb.start] = incoming
+                terminal = block_flag_state(bb, incoming)
+                if terminal != out_state[bb.start]:
+                    out_state[bb.start] = terminal
+                    changed = True
+            if not changed: break
+        # Preserve the previous emitter's behavior where the new solver has no
+        # proof. This is deliberately NOT a new interprocedural flags model.
+        # Reconstruct the old address-order meet without mixed-ZF acceptance;
+        # otherwise a legacy call could accidentally justify a new ZF join.
+        legacy_out = {}
+        for bb in blocks:
+            sources = legacy_preds[bb.start]
+            legacy_in = (merge_flag_states([legacy_out[p] for p in sorted(sources)],
+                                           allow_zf=False)
+                         if bb.start != start and sources and
+                            all(p in legacy_out for p in sources) else None)
+            legacy_out[bb.start] = block_flag_state(bb, legacy_in,
+                                                  conservative_calls=False)
+            if incoming_state[bb.start] is None:
+                incoming_state[bb.start] = legacy_in
+        self.lifter.needs_zf_snapshot = any(
+            state and state[0] in ("snapshot_zf", "snapshot_sz") for state in incoming_state.values())
+        self.lifter.needs_signed_snapshot = any(
+            state and state[0] == "snapshot_sz" for state in incoming_state.values())
+        if self.lifter.needs_zf_snapshot:
+            lines.append("    int _zf = 0; /* captured CMP/TEST/DEC/SUB zero flag */")
+        if self.lifter.needs_signed_snapshot:
+            lines.append("    int _slt = 0; /* captured CMP/TEST SF xor OF */")

-        out_state = {}
         for bb in blocks:
             # Emit label if this block is a branch target
             if bb.start in label_addrs or bb.start == start:
@@ -937,25 +975,8 @@ class FunctionTranslator:
                 # compile. The null statement costs nothing and is always valid.
                 lines.append(f"loc_{bb.start:08X}: ;")

-            # Inherit the flag state only when every predecessor agrees on it.
-            # Blocks are walked in address order, so a back edge's predecessor
-            # may not be computed yet -- treat that as unknown rather than
-            # guessing, which costs a fallback condition and never a wrong one.
-            sources = preds[bb.start]
-            if bb.start == start or not sources:
-                incoming = None
-            elif all(p in out_state for p in sources):
-                states = [out_state[p] for p in sources]
-                incoming = states[0]
-                for other in states[1:]:
-                    if other != incoming:
-                        incoming = None
-                        break
-            else:
-                incoming = None
-
-            stmts, out_state[bb.start] = lift_basic_block(
-                self.lifter, bb, flag_state=incoming)
+            stmts, _ = lift_basic_block(
+                self.lifter, bb, flag_state=incoming_state[bb.start])
             for stmt in stmts:
                 lines.append(f"    {stmt}")

@@ -964,7 +985,7 @@ class FunctionTranslator:
         # Continue into the next function when control runs off the bottom.
         if fallthrough_target is not None:
             ft_name = self.lifter._call_target_name(fallthrough_target)
-            lines.append(f"    g_seh_ebp = ebp; {ft_name}(); return;"
+            lines.append(f"    g_ebp = g_seh_ebp = ebp; {ft_name}(); return;"
                          f" /* fallthrough 0x{fallthrough_target:08X} */")
             lines.append(f"")

@@ -1378,7 +1399,7 @@ class BatchTranslator:
             "/**",
             f" * {_config.banner_name(getattr(self, 'title', None))}"
             f" - Recompiled Function Declarations",
-            f" * {stats['translated']} functions, auto-generated by tools/recomp",
+            " * Auto-generated by tools/recomp; native overrides share these declarations.",
             " */",
             "",
             "#ifndef RECOMP_FUNCS_H",
@@ -1387,23 +1408,15 @@ class BatchTranslator:
             '#include "recomp_types.h"',
             "",
         ]
-        for addr, name, _ in translations:
+        # Declaration order must not change when an SDK function becomes native:
+        # otherwise every game translation unit unnecessarily rebuilds.
+        declarations = {addr: name for addr, name, _ in translations}
+        declarations.update(manual_decls)
+        declarations.update(unresolved)
+        for addr, name in sorted(declarations.items()):
             decl = self._make_declaration(addr, name)
             header_lines.append(f"{decl};")

-        if manual_decls:
-            header_lines.append("")
-            header_lines.append("/* Hand-written overrides (defined by the project) */")
-            for addr in sorted(manual_decls):
-                header_lines.append(
-                    f"void {manual_decls[addr]}(void);  /* 0x{addr:08X} */")
-
-        if unresolved:
-            header_lines.append("")
-            header_lines.append("/* Unresolved call targets (stubbed) */")
-            for addr in sorted(unresolved):
-                header_lines.append(f"void {unresolved[addr]}(void);")
-
         header_lines.extend(["", "#endif /* RECOMP_FUNCS_H */", ""])

         with open(header_path, "w", encoding="utf-8") as f:
@@ -1461,8 +1474,12 @@ class BatchTranslator:

         # Split translations into chunks and write .c files
         generated_files = [header_path]
-        chunks = [translations[i:i+chunk_size]
-                  for i in range(0, len(translations), chunk_size)]
+        # Keep each original function's chunk stable as native overrides are
+        # introduced. An omitted body must not shift all later functions.
+        chunk_index = {addr: i // chunk_size for i, (addr, _) in enumerate(func_list)}
+        chunks = [[] for _ in range((len(func_list) + chunk_size - 1) // chunk_size)]
+        for translation in translations:
+            chunks[chunk_index[translation[0]]].append(translation)

         # Remove chunk files a previous, larger run left behind. Projects glob
         # gen/*.c into their build, so a stale chunk keeps compiling: it still
@@ -1485,8 +1502,8 @@ class BatchTranslator:
                 "/**",
                 f" * {_config.banner_name(getattr(self, 'title', None))}"
                 f" - Recompiled code chunk {ci}",
-                f" * Functions: {len(chunk)} "
-                f"(0x{chunk[0][0]:08X} - 0x{chunk[-1][0]:08X})",
+                f" * Functions: {len(chunk)} " +
+                (f"(0x{chunk[0][0]:08X} - 0x{chunk[-1][0]:08X})" if chunk else "(native overrides only)"),
                 " */",
                 "",
                 "#define RECOMP_GENERATED_CODE",
@@ -1541,7 +1558,7 @@ class BatchTranslator:
                 popped = self.translator._stub_ret_bytes(addr)
                 note = (f"ret {popped}" if popped else "not detected")
                 stub_lines.append(
-                    f"void {unresolved[addr]}(void) {{ g_esp += {4 + popped}; "
+                    f"void {unresolved[addr]}(void) {{ recomp_icall_fail_log(0x{addr:08X}u); "
                     f"/* 0x{addr:08X}: {note} */ }}"
                 )
             stub_lines.append("")
